@model List<MyOnlineClinic.Entity.Banner>
@*@{
    string height = string.Empty;
    string width = string.Empty;
}*@
<script src="~/Scripts/jquery-1.10.2.min.js"></script>

@if (Model.Count > 0)
{
    <div class="carousel carousel-1 banner-carousal slide" data-ride="carousel">
        <div class="carousel-inner">
            @foreach (var item in Model)
            {

                //if (item.BannerSize != "None")
                //{
                //    string[] arr = item.BannerSize.Split('x');
                //    if (arr.Length > 0)
                //    {
                //        width = arr[0];
                //        height = arr[1];
                //    }
                //}
                var active = string.Empty;
                if (item == Model.First())
                {
                    active = "active";
                }
                <div class="item item-dark @active">
                    <a href="@item.Url" target="_blank"><img src="/UploadedFiles/@item.FileName"/></a>
                </div>
            }
        </div>
    </div>
}


